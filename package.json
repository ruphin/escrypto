{
  "name": "escrypto",
  "private": true,
  "description": "A modern ES6 JavaScript library for basic cryptocurrency functions",
  "scripts": {
    "build": "rollup -c",
    "dev": "browser-sync start --config browsersync.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "2.26.3",
    "connect-history-api-fallback": "1.6.0"
  }
}
